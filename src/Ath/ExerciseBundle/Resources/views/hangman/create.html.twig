<div ng-controller="HangmanCreateCtrl">
  <form action="{[{formActionUrl}]}" method="POST">
    Niveau :
    <select name="level" required>
      <option ng-repeat="level in levels" value="{[{level.id}]}">{[{level.name}]}</option>
    </select>
    Matière :
    <select name="discipline" ng-model="discipline">
      <option ng-repeat="discipline in disciplines" value="{[{discipline.id}]}">{[{discipline.name}]}</option>
    </select>
    Chapitre :
    <select name="chapter" required>
      <option ng-repeat="chapter in chapters |filter: {discipline.id: discipline}" value="{[{chapter.id}]}">{[{chapter.name}]}</option>
    </select>
    <br/>
    <br/>
    Nom : <input required ng-model="exercise.name" name="name" class="form-control"/><br/>
    mot caché : <input required name="hidden_word" class="form-control"/><br/>

    <div class="row">
      <input type="submit" class="btn btn-primary"/>
    </div>
  </form>
</div>
