<div ng-controller="QcmCreateCtrl">
  <form action="{[{formActionUrl}]}" method="POST">
    Nom : <input required ng-model="exercise.name" name="name"/><br/>
    Question : <input required ng-model="exercise.question" name="question"/><br/>
    Réponses :<br/>

      <ul ng-repeat="answer in exercise.answers">
        <li>
          <input required type="radio" name="selectAnswer" ng-click="rightAnswer($index)"/>
          <input required type="text" name="answers[]" value="{[{answer.value}]}" />
          [ <a href="" ng-click="removeAnswer(answer)">X</a> ]
        </li>
      </ul>

    [ <a href="" ng-click="addAnswer()">add</a> ]
    <input type="hidden" value="{[{exercise.rightAnswer}]}" name="rightAnswer"/>
    <input type="submit"/>
  </form>
</div>
